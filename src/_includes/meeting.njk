<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css">
    <title>{{ title }}</title>
</head>
<body>
    
    {% include 'header.njk' %}
    <main class="container">
        <h1 class="meetingTitle">{{ title }}</h1>
        <div class="meetingContent">
    {{ content | safe }}
    </div>
    </main>
    {% include 'footer.njk' %}
</body>
</html>
